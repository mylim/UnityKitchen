using UnityEngine;
using System.Collections;

public class HasContent : MonoBehaviour {
    public bool hasWater;
    public bool hasTeaBag;

    // Use this for initialization
    void Start()
    {
        hasWater = false;
        hasTeaBag = false;
    }
}
